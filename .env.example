# Root env (optional)
# Most config lives in `server/.env`

# If you run the server from repo root tooling, you can also set these here.
# The server reads its own `.env` first.

MAPLE_PROXY_URL=http://127.0.0.1:1234
MAPLE_API_KEY=sk-your-key-here
MAPLE_MODEL=llama-3.3-70b
